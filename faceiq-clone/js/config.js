// Local development only — fill these values to test auth with ./serve.sh
// On Vercel, keys are injected automatically via /api/config.js
window.APP_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  redirectAfterLogin: "/app"
};
